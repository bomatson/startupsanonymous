.row
  .large-6.columns
    %h2 Edit your profile
    = form_for @user, url: user_path, html: {class: 'custom user-editing'} do |f|
      = f.label :email
      = f.text_field :email
      = f.label :anonymous?
      = f.check_box :anonymous
      = f.label :skype_username
      = f.text_field :skype
      = f.submit :submit, class: 'button prefix'
    %p= link_to "Please click here if you need to reset your password", new_password_reset_path