.row.calendar-heading 
  .large-8.columns
    %h2 Listener Availability
    %h4.subheader Please click on a timeslot to book it
    - if @closest.present?
      %h4.subheader
        Next available timeslot:
        = link_to @closest.formatted_start_time, @closest
.row 
  = render partial: 'calendar'